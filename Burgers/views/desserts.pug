extends layout

block vars
    -var title = "Desserts";

block content
    each picture in desserts.pictures
        div
            p #{picture[0]}: #{picture[2]}
            img(src=picture[1], class="image")